<form method="post" action="{{ path('app_presentateur_delete', {'id': presentateur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ presentateur.id) }}">
    <button class="btn">Delete</button>
</form>
