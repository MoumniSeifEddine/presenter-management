{% extends 'base.html.twig' %}

{% block title %}Presentateur{% endblock %}

{% block body %}
    <h1>Presentateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ presentateur.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ presentateur.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_presentateur_index') }}">back to list</a>

    <a href="{{ path('app_presentateur_edit', {'id': presentateur.id}) }}">edit</a>

    {{ include('presentateur/_delete_form.html.twig') }}
{% endblock %}
