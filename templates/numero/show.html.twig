{% extends 'base.html.twig' %}

{% block title %}Numero{% endblock %}

{% block body %}
    <h1>Numero</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ numero.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ numero.titre }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ numero.duree ? numero.duree|date('H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_numero_index') }}">back to list</a>

    <a href="{{ path('app_numero_edit', {'id': numero.id}) }}">edit</a>

    {{ include('numero/_delete_form.html.twig') }}
{% endblock %}
